{
  "name": "rapid-ts",
  "description": "(R)EST (A)PI (P)rovisioning and (I)ntegration with JSON (D)ata",
  "scripts": {
    "minify": "",
    "compile": "node helper/buildHelper.js && tsc --removeComments && tsc --build tsconfig.es5.json && npm run minify",
    "publish": "npm run compile && node helper/increaseVersion.js && cd ./dist && npm publish",
    "setup-example": "npm run compile && cd dist && npm link && cd ../example && npm link @rapid-api/rapid-ts && node index.js"
  },
  "author": "shukaaa",
  "license": "MIT",
  "dependencies": {
    "body-parser": "^1.20.2",
    "express": "^4.18.2",
    "fs-extra": "^11.1.1",
    "versionup-js": "^1.0.1"
  },
  "devDependencies": {
    "@types/express": "^4.17.17",
    "@types/node": "^20.3.0",
    "typescript": "^5.1.3"
  }
}
